ALTER TABLE productos DROP FOREIGN KEY FK_padre;
ALTER TABLE productos DROP INDEX IDX_Productos;
ALTER TABLE `productos` DROP `producto_padre`

ALTER TABLE `remisiones_producto` ADD COLUMN `id_pedido` varchar(255) DEFAULT NULL;
ALTER TABLE `remisiones_producto`
  ADD KEY `IDX_Pedido` (`id_pedido`);
  
ALTER TABLE `remisiones_producto`
ADD CONSTRAINT `FK_id_pedido` FOREIGN KEY (`id_pedido`) REFERENCES `pedidos` (`id`);

UPDATE estados SET ELIMINADO = '1'

INSERT INTO `estados` (`ID`, `ELIMINADO`, `ESTADO`, `MODULO`) VALUES
('ORDERS_INPROCESS', 0, 'PRODUCTOS EN PROCESO', 'ORDERS'),
('ORDERS_ACCEPTED', 0, 'ACEPTADA', 'ORDERS'),
('ORDERS_APPLY', 0, 'SOLICITADA', 'ORDERS'),
('ORDERS_CANCELED', 0, 'CANCELADA', 'ORDERS'),
('ORDERS_DELIVERED', 0, 'ENTREGADA', 'ORDERS'),
('ORDERS_ENDED', 0, 'PRODUCTOS TERMINADOS', 'ORDERS'),
('ORDERS_PAID', 0, 'PAGADA', 'ORDERS'),
('ORDERS_PARTIALDELIVERY', 0, 'ENTREGA PARCIAL', 'ORDERS'),
('ORDERS_PARTIALPAID', 0, 'PAGADA PARCIAL', 'ORDERS'),
('REMISSIONS_DELIVERED', 0, 'ENTREGADA', 'REMISSIONS'),
('REMISSIONS_INPROCESS', 0, 'EN PROCESO', 'REMISSIONS');


UPDATE `pedidos` SET estado = 'ORDERS_ACCEPTED'
UPDATE `remisiones` SET estado = 'REMISSIONS_DELIVERED'
DELETE FROM estados WHERE ELIMINADO = '1'

ALTER TABLE remisiones DROP FOREIGN KEY FK_remisiones_id_pedido;
ALTER TABLE remisiones DROP INDEX FK_remisiones_id_pedido;
